const sqlite3 = require("sqlite3").verbose(); const path = require("path"); const dbPath = path.resolve(__dirname, "database", "testimonios.db"); const db = new sqlite3.Database(dbPath); db.run(` CREATE TABLE IF NOT EXISTS testimonios ( id INTEGER PRIMARY KEY AUTOINCREMENT, nombre TEXT NOT NULL, mensaje TEXT NOT NULL, fecha TEXT NOT NULL, foto TEXT, rating INTEGER DEFAULT 5 ) `); module.exports = db;